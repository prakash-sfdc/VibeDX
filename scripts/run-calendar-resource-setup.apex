// Creates the public group "Sales Reps" for calendar/resource sharing.
// Then create the calendar and resource in Setup (see docs/Calendar_And_Resource_Assignment.md).
CalendarAndResourceCreator.createSalesRepsGroup();
